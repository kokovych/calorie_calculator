

export default function handleAuthREsponse(response) {
    // 1) make response. if 200 - ok, return response
    // 2) if not 200 - try to update accessToken with refresh Token
    // 3) if still not 200 - user is not authorized


}
